/// display_text(text, color, time);
var text = argument0,
    color = argument1,
    time = argument2;

objDisplay._text = text;
objDisplay._color = color;
objDisplay._decayMax = time;
objDisplay._decay = time;
